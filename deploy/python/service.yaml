apiVersion: v1
kind: Service
metadata:
  name: dev-env-python
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: http
    protocol: TCP
    name: http
  - port: 5678
    targetPort: debug
    protocol: TCP
    name: debug
  selector:
    app: dev-env-python
