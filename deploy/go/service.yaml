apiVersion: v1
kind: Service
metadata:
  name: dev-env-go
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    name: http
  - port: 2345
    targetPort: debug
    name: debug
  selector:
    app: dev-env-go
